import { Colors } from '@wisetail/tokens';

export default {
  minHeight: '20em',
  borderRadius: '3px',
  border: `1px solid ${Colors.SILVER_20}`,
  fontFamily: '"Fira code", monospace',
  fontSize: '1.6rem'
}
